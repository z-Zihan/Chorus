{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the AgentLink desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "process:allow-restart",
    "shell:allow-open",
    "updater:default"
  ]
}
