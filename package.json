{
  "name": "chorus",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "concurrently -k -n server,web -c magenta,cyan \"pnpm --filter @chorus/server dev\" \"pnpm --filter @chorus/web dev\"",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "db:generate": "pnpm --filter @chorus/server db:generate",
    "db:migrate": "pnpm --filter @chorus/server db:migrate",
    "db:studio": "pnpm --filter @chorus/server db:studio",
    "build": "pnpm --filter @chorus/shared build && pnpm --filter @chorus/relay build && pnpm --filter @chorus/server build && pnpm --filter @chorus/web build",
    "test": "pnpm --filter @chorus/shared test && pnpm --filter @chorus/server test",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "start": "pnpm --filter @chorus/server start"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@eslint/js": "^9.39.5",
    "@tauri-apps/cli": "^2.0.0",
    "concurrently": "^9.2.0",
    "eslint": "9",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.65.0"
  }
}
